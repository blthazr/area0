---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Receiver
metadata:
  name: home-ops
spec:
  events:
    - "ping"
    - "push"
  resources:
    - kind: GitRepository
      name: flux-system
  secretRef:
    name: github-webhook-token
  type: github
