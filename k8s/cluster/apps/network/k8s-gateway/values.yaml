---
nameOverride: &app k8s-gateway
domain: "${SECRET_DOMAIN}"
fullnameOverride: *app
service:
  annotations:
    metallb.universe.tf/loadBalancerIPs: "${SVC_K8S_GATEWAY_ADDRESS}"
  externalTrafficPolicy: Local
  port: 53
  type: LoadBalancer
ttl: 1
