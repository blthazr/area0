---
nameOverride: &app metallb
crds:
  enabled: false
prometheus:
  podMonitor:
    enabled: true
  prometheusRule:
    enabled: true
  namespace: observability
  serviceAccount: prometheus-prometheus
