---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - dashboard
  - helm-release.yaml
configMapGenerator:
  - name: cloudflare-proxy-networks
    files:
      - cloudflare-proxy-networks.txt
generatorOptions:
  disableNameSuffixHash: true
