---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/instance: &app prowlarr
  app.kubernetes.io/name: *app
configurations:
  - kustomize-config.yaml
configMapGenerator:
  - name: prowlarr-helm-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - persistent-volume-claim.yaml
  - helm-release.yaml
  - secret.sops.yaml
