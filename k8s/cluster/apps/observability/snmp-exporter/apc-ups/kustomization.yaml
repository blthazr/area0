---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - kustomize-config.yaml
configMapGenerator:
  - name: snmp-exporter-apc-ups-helm-values
    files:
      - values.yaml
resources:
  - config-map.yaml
  - helm-release.yaml
  - prometheus-rule.yaml
