---
nameOverride: &app vpa
admissionController:
  enabled: false
recommender:
  enabled: true
  extraArgs:
    prometheus-address: |-
      http://kube-prometheus-stack-prometheus.observability.svc.cluster.local:9090
    storage: prometheus
updater:
  enabled: false
