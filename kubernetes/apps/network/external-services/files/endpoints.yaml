---
apiVersion: v1
kind: Endpoints
metadata:
  name: &app files
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
subsets:
  - addresses:
      - ip: "${NAS_ADDRESS}"
    ports:
      - name: http
        port: 7001
        protocol: TCP
