---
apiVersion: v1
kind: Endpoints
metadata:
  name: &app pi-hole
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
subsets:
  - addresses:
      - ip: "${PIHOLE_ADDRESS}"
    ports:
      - name: http
        port: 80
        protocol: TCP
