---
apiVersion: v1
kind: Service
metadata:
  name: &app pi-hole
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  clusterIP: None
  ports:
    - name: http
      port: &port 80
      protocol: TCP
      targetPort: *port
  type: ClusterIP
