---
apiVersion: v1
kind: Service
metadata:
  name: &app protect
spec:
  externalName: "${UNIFI_PROTECT_ADDRESS}"
  ports:
    - name: https
      port: &port 443
      protocol: TCP
      targetPort: *port
  type: ExternalName
