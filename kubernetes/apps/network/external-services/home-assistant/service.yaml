---
apiVersion: v1
kind: Service
metadata:
  name: &app home-assistant
spec:
  externalName: "${HOME_ASSISTANT_ADDRESS}"
  ports:
    - name: http
      port: &port 8123
      protocol: TCP
      targetPort: *port
  type: ExternalName
