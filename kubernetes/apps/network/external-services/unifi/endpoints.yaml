---
apiVersion: v1
kind: Endpoints
metadata:
  name: &app unifi
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
subsets:
  - addresses:
      - ip: "${UNIFI_NETWORK_ADDRESS}"
    ports:
      - name: https
        port: 443
        protocol: TCP
