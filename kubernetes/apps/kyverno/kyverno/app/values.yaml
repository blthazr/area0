---
nameOverride: &app kyverno
crds:
  install: true
topologySpreadConstraints:
  - labelSelector:
      matchLabels:
        app.kubernetes.io/instance: kyverno
    maxSkew: 1
    topologyKey: kubernetes.io/hostname
    whenUnsatisfiable: DoNotSchedule
