---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app mealie
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      version: 1.5.1
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
    - name: volsync
      namespace: volsync
  install:
    remediation:
      retries: 3
  interval: 30m
  maxHistory: 2
  uninstall:
    keepHistory: false
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    env:
      TZ: "${CLUSTER_TIME_ZONE}"
    image:
      repository: docker.io/hkotel/mealie
      tag: frontend-nightly@sha256:bcf8c9736f7f488841282c2883ad5fab24a98193ffa8d731c98130acc4527870
    ingress:
      main:
        annotations:
          external-dns.home.arpa/enabled: "true"
          hajimari.io/appName: Recipes
          hajimari.io/group: Home
          hajimari.io/icon: mdi:food
          hajimari.io/info: Recipe Manager
        enabled: true
        hosts:
          - host: &host "recipes.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        ingressClassName: nginx
        tls:
          - hosts:
              - *host
    nodeSelector:
      node-role.kubernetes.io/worker: "true"
    persistence:
      api-data:
        enabled: true
        existingClaim: mealie-config-v2
      backup:
        enabled: true
        mountPath: /api-data/backups
        path: "${NAS_NFS_PATH}/backup/app/mealie"
        server: "${NAS_ADDRESS}"
        type: nfs
    service:
      main:
        ports:
          api:
            enabled: true
            port: 9000
          http:
            port: 3000
    sidecars:
      api:
        env:
          - name: ALLOW_SIGNUP
            value: "false"
          - name: API_PORT
            value: "9000"
          - name: AUTO_BACKUP_ENABLED
            value: "true"
          - name: BASE_URL
            value: "https://recipes.${SECRET_DOMAIN}"
          - name: DEFAULT_EMAIL
            value: "admin@${SECRET_DOMAIN}"
          - name: TOKEN_TIME
            value: 720
          - name: TZ
            value: "${CLUSTER_TIME_ZONE}"
        name: mealie-api
        image: docker.io/hkotel/mealie:api-nightly@sha256:e80ea8dea109be0841cd27b8be4583f399be789f7cf993e317e0ad51f86c42dd
        imagePullPolicy: IfNotPresent
        ports:
          - name: api
            containerPort: 9000
        volumeMounts:
          - name: api-data
            mountPath: /app/data/
          - name: backup
            mountPath: /app/data/backups
