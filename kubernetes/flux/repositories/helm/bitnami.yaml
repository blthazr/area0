---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: bitnami-charts
  namespace: flux-system
spec:
  interval: "${HELM_REPO_DEFAULT_INTERVAL}"
  timeout: "${HELM_REPO_DEFAULT_TIMEOUT}"
  url: https://charts.bitnami.com/bitnami
