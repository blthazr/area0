---
apiVersion: v1
kind: Service
metadata:
  name: &app unifi
spec:
  externalName: "${UNIFI_NETWORK_ADDRESS}"
  ports:
    - name: https
      port: &port 443
      protocol: TCP
      targetPort: *port
  type: ExternalName
