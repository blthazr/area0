# Source for `mise run secrets:env`. Add new tools by appending a commented section.
# Resolves to .secrets.env (gitignored). Mise loads .secrets.env on `mise exec`.

NET_SERVICES_CIDR="op://area0/cluster-secrets/NET_SERVICES_CIDR"
SVC_K8S_API="op://area0/cluster-secrets/SVC_K8S_API"
SECRET_DOMAIN="op://area0/cluster-secrets/EXTERNAL_DOMAIN"
