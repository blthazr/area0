# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#   @file           :   defaults/main.yml
#   @description    :   defaults for ansible-role-os
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
---
# --------------------------------------------------------------------------------------------------
# time settings
# --------------------------------------------------------------------------------------------------
# -- (str) timezone for the os
os_timezone: "{{ timezone | default('America/Chicago') }}"
# ==================================================================================================

# --------------------------------------------------------------------------------------------------
# host settings
# --------------------------------------------------------------------------------------------------
# -- (str) domain for the host
domain: localdomain
# ==================================================================================================

# --------------------------------------------------------------------------------------------------
# reboot settings
# --------------------------------------------------------------------------------------------------
# -- (bool) reboot host if needed
reboot_if_needed: true
# ==================================================================================================

# --------------------------------------------------------------------------------------------------
# operating system packages
# --------------------------------------------------------------------------------------------------
# -- (dict) operating system packages
os_packages:

  # -- (dict) operating system family
  debian:

    # -- (list) flat list of packages to be installed with apt
    apt_install:
      - apt-transport-https
      - ca-certificates
      - curl
      - htop
      - nano
      - nfs-common
      - ntpdate
      - psmisc
      - software-properties-common
      - unattended-upgrades
      - unzip
      - python3-apt
      - python3-reportbug
      - python3-software-properties
      - python3
      - python
      - sudo

    # -- (list) flat list of packages to be uninstalled with apt
    apt_remove:
      - bcache-tools
      - btrfs-progs
      - fwupd
      - open-vm-tools
      - tmux
# ==================================================================================================
